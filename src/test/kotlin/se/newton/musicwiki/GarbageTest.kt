package se.newton.musicwiki

import org.junit.jupiter.api.Test

/**
 * This is a placeholder just so the test folder has something to run until we add the real tests
 */
class GarbageTest {
    @Test
    fun placeholderUnitTest() {
    }
}